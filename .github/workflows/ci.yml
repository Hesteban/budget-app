name: CI

on:
  push:
    branches: ["main", "feature/**"]
  pull_request:
    branches: ["main"]

jobs:
  test:
    name: Unit tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"
          cache-dependency-path: requirements-test.txt

      - name: Install dependencies
        run: pip install -r requirements-test.txt

      - name: Run pytest
        env:
          APP_ENV: test        # activates FakeRepository; no Supabase creds needed
        run: pytest tests/ -v --tb=short

# ---------------------------------------------------------------------------
# E2E tests with Playwright — stubbed, ready for the run-e2e-tests phase
# ---------------------------------------------------------------------------
# Uncomment this job when the Playwright test suite is ready.
#
#  e2e:
#    name: E2E tests (Playwright)
#    runs-on: ubuntu-latest
#    needs: test           # only run if unit tests pass
#
#    steps:
#      - name: Checkout
#        uses: actions/checkout@v4
#
#      - name: Set up Python 3.12
#        uses: actions/setup-python@v5
#        with:
#          python-version: "3.12"
#          cache: "pip"
#          cache-dependency-path: requirements-test.txt
#
#      - name: Install dependencies
#        run: |
#          pip install -r requirements-test.txt
#          playwright install --with-deps chromium
#
#      - name: Start Streamlit server (background)
#        env:
#          APP_ENV: test
#        run: |
#          streamlit run main.py \
#            --server.port 8501 \
#            --server.headless true \
#            --browser.gatherUsageStats false &
#          # Wait until the server is accepting connections
#          npx wait-on http://localhost:8501 --timeout 60000
#
#      - name: Run Playwright E2E tests
#        env:
#          APP_ENV: test
#          BASE_URL: http://localhost:8501
#        run: pytest tests/e2e/ -v --tb=short
